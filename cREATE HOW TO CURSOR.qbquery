CREATE PROCEDURE HOW_TO_DECLARE_CURSOR(IN CATEGORY_ID INT)
BEGIN
  DECLARE CATEGORY_NAME VARCHAR(15);
  DECLARE DESCRIPTION LONGTEXT;
  DECLARE MY_CURSOR CURSOR FOR SELECT CategoryName,Description from TBL_CATEGORIES WHERE (CategoriesID= CATEGORY_ID);
END